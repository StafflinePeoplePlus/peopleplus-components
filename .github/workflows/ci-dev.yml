name: CI Dev

on:
  push:
    branches-ignore:
      - uat
      - main
  pull_request:
    branches-ignore:
      - uat
      - main

concurrency:
  group: dev

jobs:
  build:
    name: Run Tests

    strategy:
      matrix:
        os-version: [ubuntu-latest]
        node-version: [18.x]

    runs-on: ${{ matrix.os-version }}

    steps:
      - name: Checkout 🛒
        uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }} 🛒
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install Node Packages 🛒
        run: npm ci

      - name: Lint 🧽
        run: npm run lint

      - name: Run Vi tests 🏃
        run: npm run test:unit

      - name: Install playwright browsers 🛒
        run: npx playwright install --with-deps

      - name: Run Playwrite tests 🏃
        run: npm run test

      - name: Build app 🧱
        run: npm run build

      - name: Launch app 🚀
        run: npm run preview &

      - name: Wait for app to start ☕️
        run: sleep 10

      - name: OWASP ZAP Full Scan - ${{ github.head_ref || github.ref_name }} 🩻
        uses: zaproxy/action-full-scan@v0.4.0
        with:
          issue_title: 'ZAP Full Scan Report - ${{ github.head_ref || github.ref_name }}'
          target: 'http://localhost:4173'
          rules_file_name: '.zap/rules-dev.tsv'

      - name: OWASP ZAP Full Scan - ${{ github.head_ref || github.ref_name }} AJAX 🩻
        uses: zaproxy/action-full-scan@v0.4.0
        with:
          issue_title: 'ZAP Full Scan Report - ${{ github.head_ref || github.ref_name }} AJAX'
          target: 'http://localhost:4173'
          rules_file_name: '.zap/rules-dev.tsv'
          cmd_options:
            #AJAX Spider
            '-j'

      - name: Accessibility Scan - Dev 🩻
        uses: a11ywatch/github-actions@v1.14.1
        with:
          WEBSITE_URL: '$(hostname):4173'
          SUBDOMAINS: false
          LIST: true
